{{ .Chart.Name }} {{ .Chart.AppVersion }} installed as release "{{ .Release.Name }}" in namespace {{ .Release.Namespace }}.

{{ if .Values.ingress.enabled -}}
The application is reachable at http://{{ .Values.ingress.host | default "localhost" }}
{{- else -}}
No ingress was created. Reach the frontend with:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "url-shortener.componentName" (dict "root" $ "component" "frontend") }} 8080:80
{{- end }}

Running {{ .Values.api.replicaCount }} API replica(s) at log level {{ .Values.config.logLevel }}.

Watch the rollout:

  kubectl get pods -n {{ .Release.Namespace }} -w

Roll back to the previous release:

  helm rollback {{ .Release.Name }} -n {{ .Release.Namespace }}
